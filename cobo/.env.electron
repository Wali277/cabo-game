# Production server URL for multiplayer in the Electron desktop app.
# Vite bakes this value into the build when you run:  vite build --mode electron
#
# Change this to match whichever platform is currently hosting your server,
# then re-run  npm run electron:build  to pick up the new URL.
#
# Render deployment:   https://cabo-game-r5xb.onrender.com  (current/live — verified 2026-06-03)
# Fly.io deployment:   https://cobo-game.fly.dev
VITE_SERVER_URL=https://cabo-game-r5xb.onrender.com
